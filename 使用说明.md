# 财务面试练习室

为财务管理专业求职者准备的中文模拟面试工具。在线部署和访问后台见《部署与后台.md》。

## 直接使用

双击 `dist/财务面试练习室.html`，使用 Chrome、Edge 或 Safari 打开。这个文件已包含程序和题库，可以直接发送给朋友。电脑无需安装软件、注册账号或填写 API 密钥。

程序适配手机屏幕。手机需要在支持运行 HTML 的浏览器环境中打开；微信、文件应用中的附件预览可能不会执行程序。也可以把文件放到静态网页托管服务后，通过网址访问。

## 怎么练

1. 选择财务会计、财务分析、预算与成本、资金与出纳或审计助理。
2. 选择 6 题或 10 题。初次使用建议选“边练边复盘”；面试前选“完整模拟面试”。
3. 输入回答，或先口头说一遍，再记下关键内容。页面计时仅作节奏参考，超时不会强制提交。
4. 练习模式每题提交后查看参考思路、易错点和追问，勾选自己确实讲清楚的要点；模拟模式在整场结束后逐题复盘。
5. 查看复盘报告，优先重练跳过题和尚未掌握的要点。可导出 Markdown 格式的完整复盘，包含自己的回答和参考思路。

## 功能说明

- “保存并退出”或关闭页面后，在同一个浏览器、相同文件位置/网址重新打开，可继续未完成练习。
- 计时在点击暂停、退出面试页、进入复盘、打开弹窗或页面处于后台时停止。
- 题库可以按关键词、岗位、题型筛选；收藏题目后可在“我的收藏”找到。
- “朗读题目”使用浏览器的语音能力，是否有中文语音取决于设备；再次点击可停止朗读。默认使用文字作答，没有录音或语音识别。
- 反馈为参考思路、表达提示与人工自评。“自评要点覆盖率”是已勾选要点占已自评题目要点的比例；跳过题和未自评题不计入。当前版本没有连接 AI 服务，不会自动判断回答的专业正确性。
- 行为题参考答案提供表达结构，请用自己的真实经历填充，不要照搬示例为个人经历。
- 记录保存在当前浏览器，最多保留最近 30 场已完成面试。清理浏览器数据、更换浏览器或移动文件位置后，记录可能无法沿用；重要内容请及时导出。
- 程序不上传回答。单文件离线版不发送访问记录；在线部署配置了访问服务后，由后台保存来源 IP 等访问日志。语音朗读由浏览器/系统提供，所用语音是否联网取决于设备。

## 项目开发

程序运行不依赖第三方包，`index.html` 可直接打开。

```sh
npm install
npm test
npm run build
npm start
```

开发预览默认在 `http://127.0.0.1:43187`。`npm run build` 生成可分享的单个 HTML 文件。浏览器自动测试需要本机 Chrome；可运行 `npm run test:browser`。

项目结构：`src/questions.js` 是题库，`src/engine.js` 是组卷与复盘逻辑，`src/app.js` 是交互，`src/styles.css` 是样式。修改后重新构建单文件版本。
