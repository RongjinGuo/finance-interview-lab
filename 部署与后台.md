# 部署、账号与后台维护

## 当前账号版

账号版已部署于 [Rongjin03/finance-interview-lab](https://huggingface.co/spaces/Rongjin03/finance-interview-lab)，使用独立应用地址打开和登录：

[https://rongjin03-finance-interview-lab.hf.space/](https://rongjin03-finance-interview-lab.hf.space/)

2026 年 9 月 11 日已完成部署，并通过真实线上登录、答题保存、第二个浏览器恢复、管理员查看回答和 Space 重启后恢复的验证。另行读取私有 GitHub 仓库确认了记录确实已推送。用户手机网络能否直连这个新域名仍需实机确认，电脑验证不能替代手机网络测试。

同日已切换为邀请码登录，按确认的权限关联原练习账号和管理员。两个邀请码均已完成线上登录、权限和原记录校验；包含前导零的邀请码可完整使用。邀请码版再次通过实际保存和新浏览器读取验证。

公开源码仍在 [RongjinGuo/finance-interview-lab](https://github.com/RongjinGuo/finance-interview-lab)。账号、邀请码校验值和答题记录保存在单独的私有仓库 `RongjinGuo/finance-interview-records`。登录只需邀请码；每个邀请码绑定一个现有账号，因此原答题记录和本机草稿可继续使用。

| 组成 | 用途 |
| --- | --- |
| HF Docker Space | 运行网页、登录、用户管理和保存接口 |
| 私有 GitHub 记录仓库 | 长期保存账号与每位用户的完整练习状态 |
| 浏览器账号草稿 | 即时保存本机修改与待同步请求，按用户名和标签页分别隔离 |
| 管理员入口 | 登录后点击“用户与记录”，创建用户并查看逐题回答 |

使用已有 PRO 账户的 CPU Basic，没有升级到按小时计费的硬件。Space 文件系统是临时存储，重启或重建后的持久化来源是私有 GitHub 仓库。PRO 订阅与平台其他资源的费用按账户现有方案计费。

## 登录信息与用户管理

管理员及初始用户的登录说明保存在本机：

`~/.local/share/finance-interview-lab/accounts/账号登录信息.md`

该文件包含邀请码及对应权限，仅在本机查看和保管。公开文档、源码和构建产物不包含实际使用的邀请码。

管理员登录后可创建普通用户，用户名为 3–32 位小写字母、数字、下划线或连字符，并以字母或数字开头；邀请码为 4–12 位数字，各账号不能重复使用同一个邀请码。使用“用户与记录”查看各用户已同步的当前练习、最近 30 场历史面试、逐题回答和自评，并导出 JSON。

登录会话有效期为 12 小时，退出或服务重启会使会话失效。用户重新登录后读取同一账号的已保存记录。

## 保存与恢复机制

1. 输入先写入当前浏览器的账号草稿，不混用匿名版或其他账号的数据。
2. 停止编辑约 2 秒后发起云端保存；持续编辑时最多约 30 秒发起一次。提交回答、保存并退出和“立即保存”也会触发同步。
3. 服务检查版本、写入私有仓库并成功推送后，页面才显示“已同步到云端”。网络中断或推送失败时保留待同步内容。
4. 重试沿用同一个请求标识，避免因响应丢失而重复保存。若另一台设备更新了版本，会保留本机草稿，要求用户明确选择覆盖云端或载入云端。
5. 读取和保存请求携带草稿所属账号，服务核对该账号与当前会话一致，避免其他标签页切换登录后串号。各标签页分别保留本机草稿；登录后可从草稿恢复面板显式载入已关闭页面的草稿，并保留原备份。

每个用户的完整状态保存于 `users/<username>/state.json`，包括版本、更新时间、练习设置、未完成练习、最多 30 场已完成面试和收藏。浏览器的“下载草稿”可保留本机备份；管理员导出的 JSON 适合检查和归档，目前没有界面导入按钮。

## 账号版配置与更新

运行环境为 Node.js 24 或更新版本。使用以下命令检查并生成 HF 发布目录：

```sh
npm ci
npm test
node --test tests/account-browser/*.test.cjs
npx playwright test --config playwright.accounts.config.cjs
node scripts/build-space.mjs
```

将生成的 `space/` 内容发布到 HF Space 仓库，由其中的 Dockerfile 构建并启动服务。该目录只从明确的源码列表生成，不打包邀请码、私钥、环境文件或私有记录。

Space 配置项：

| 配置 | 用途 |
| --- | --- |
| `PUBLIC_ORIGIN` | 独立应用来源 `https://rongjin03-finance-interview-lab.hf.space`，不带末尾斜杠 |
| `GIT_REMOTE_URL` | 私有记录仓库的 SSH 地址，经 `ssh.github.com:443` 连接 |
| `GIT_PRIVATE_KEY` | 仅授权私有记录仓库读写的 SSH deploy key，放入 Space Secrets |
| `GIT_KNOWN_HOSTS` | 从 GitHub 官方元数据取得并固定的 SSH 主机公钥 |
| `INVITE_CODE_SECRET` | 至少 32 字符的服务端随机密钥，用于计算邀请码 HMAC-SHA256，放入 Space Secrets |
| `INITIAL_USERS_JSON` | 初始账号和邀请码校验值，放入 Space Secrets；只在仓库没有账号时初始化 |
| `GIT_DATA_DIR` | 临时 Git 工作目录，Docker 默认 `/home/node/data` |
| `GIT_AUTHOR_NAME` / `GIT_AUTHOR_EMAIL` | 记录仓库提交者信息，可使用默认值 |

运行密钥使用仓库范围的 deploy key，不需要给服务提供 GitHub 账户级令牌。首次初始化后通过管理员界面新增用户；修改 `INITIAL_USERS_JSON` 不会覆盖已经存在的账号。

`accounts.json` 的 `inviteCodeHash` 是使用 `INVITE_CODE_SECRET` 计算的校验值。保留该密钥才能继续校验现有邀请码，轮换密钥时需同时更新所有邀请码校验值。旧密码哈希允许保留以兼容原记录格式，但用户名和密码已不能用于登录。登录失败次数按连接来源统一限制，换一个邀请码不能绕过限制。

`GET /api/health` 在记录仓库初始化完成、服务可用后返回成功。发布后的验收应包括独立地址登录、填写并保存回答、第二个浏览器读取同一回答、管理员查看记录，以及服务重启后再次登录读取记录。一次健康检查不能代替完整保存验证。

HF 账号版通过同源接口保存数据，停用原 Cloudflare 访客请求。百度统计尚未启用。

## 保留的原 Pages 与访客服务

原服务继续保留，入口如下：

- 原面试网址：[GitHub Pages](https://rongjinguo.github.io/finance-interview-lab/)，无需账号，回答保存在本机。
- 原访客后台：[Cloudflare Worker](https://finance-interview-visits.finance-interview-lab.workers.dev/)，使用独立后台密码查看访问记录。

原 Pages 与 Cloudflare 服务曾完成部署和后台验证，但手机访问 Cloudflare 服务时出现连接失败，因此账号保存方案改为 HF 与私有 GitHub 仓库。原有服务和记录保留，不作为账号版保存成功的依据。原网址配置保存在 `deployment.json`；未配置 `visitEndpoint` 时，Pages 不发出访问记录请求。

网页每次完整加载记录一次请求，包括访问时间、来源 IP、IP 推测地区、浏览器、系统和页面路径。不保存面试回答、链接里的查询参数或完整来源页面。页面内部切换题目不重复计数。广告拦截器、网络失败或关闭脚本可能使部分访问无法记录。

后台的“独立 IP”不是独立人数；多人可能共享一个出口 IP，同一个人也可能使用多个 IP。IP 地区可能是代理、VPN 或运营商出口所在地，并非准确位置。本地测试只显示本机或内网地址。

默认保留90天访问记录，上限100000条。后台可按日期、IP或地区筛选，并导出 CSV。日期筛选按北京时间计算。

### 原访客服务首次部署

1. 登录 GitHub 和 Cloudflare；GitHub Pages 需要启用 Actions 部署。
2. 运行 `node scripts/setup-admin.mjs` 创建管理员凭据。密码保存在本机用户目录 `.local/share/finance-interview-lab/后台登录信息.md`，不会输出到日志或提交到仓库。
3. 在 Cloudflare 创建 D1 数据库，将返回的真实 ID 写入 `worker/wrangler.jsonc` 中的数据库绑定。
4. 应用数据库结构，构建后台样式，然后部署 Worker。用本机 `worker-secrets.json` 向 Cloudflare 写入 `ADMIN_PASSWORD_HASH` 和 `SESSION_SECRET`，不要把明文密码放到 Worker 配置文件。
5. 将 Worker 的实际 `/api/visit` 地址写入 `deployment.json` 的 `visitEndpoint`，推送 GitHub；Actions 自动发布网页。
6. 打开线上面试网址，再登录后台确认出现访问记录。

### 原访客服务维护命令

```sh
npx wrangler login
npx wrangler d1 create finance-interview-visits-db --config worker/wrangler.jsonc
npx wrangler d1 migrations apply DB --remote --config worker/wrangler.jsonc
npm run build:site
npx wrangler deploy --config worker/wrangler.jsonc --secrets-file /absolute/path/to/worker-secrets.json
```

部署时的密钥文件是本机用户目录 `.local/share/finance-interview-lab/worker-secrets.json`。文件路径包含空格时需要加引号。

### 原访客后台本地测试

```sh
node scripts/setup-admin.mjs
npm run build:site
npx wrangler d1 migrations apply DB --local --config worker/wrangler.jsonc
npm run worker:dev -- --port 8787
```

本地配置 `worker/.dev.vars` 不会上传，也不会提交到 GitHub。线上客户端 IP 由 Cloudflare 提供，程序不会信任浏览器自行填写的 IP。

### 原访客服务安全与费用

线上后台强制使用 HTTPS 加密连接。登录使用密码派生哈希、受保护的会话 Cookie 和失败次数限制。访问数据与 CSV 导出需要登录；退出后原会话失效。不要把后台密码分享给访客。

这套方案不需要购买常驻云服务器。实际费用与额度以 GitHub 和 Cloudflare 账户当前套餐为准，程序不会自动为你升级套餐。数据清理限制的是存储量，并不代替平台请求额度限制。
